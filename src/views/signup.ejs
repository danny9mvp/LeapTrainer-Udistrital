<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Registro</title>
    <% include partials/head %>
  </head>
  <body class="signup">
    <div class="container">
      <div class="col-sm-6 mx-auto">
        <div class="card mt-5">
          <div class="card-body">
            <!-- messages -->
            <h1 class="text-center">
              <span class="fa fa-sign-in"></span> Formulario de Registro
            </h1>
            <% if (message.length > 0) { %>
              <div class="alert alert-danger">
                <%= message %>
              </div>
            <% } %>

            <!-- form -->
            <form id="formSignup" action="/signup" method="post">
              <div class="form-group">
                <label for="email">Correo electrónico</label>
                <input type="email" id="txtEmail" name="email" class="form-control" required="true">
              </div>
              <div class="form-group">
                <label for="password">Contraseña</label>
                <input type="password" id="txtPwd" name="password" class="form-control" required="true">
              </div>
              <input type="submit" id="btnSubmit" name="submit" class="btn btn-dark btn-lg">
            </form>

            <hr>
            <div class="text-center">
              <p>¿Ya tienes una cuenta? <a href="/login">Inicia sesión</a></p>
              <p>Regresar a <a href="/">inicio</a>.</p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>